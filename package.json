{
	"name": "drag-n-drop",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"start": "node server/server.js",
		"server": "npm dev --prefix server",
		"client": "npm start --prefix client",
		"dev": "concurrently \"npm run dev --prefix server\" \"npm run client\"",
		"import": "npm run import --prefix server"
	},
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"concurrently": "^6.0.0"
	}
}
